This folder contains mainly functional components

Each component will contain:
- a stateless view component
- a component container with will contain the view component and pass the data
in the form of props to the child(view) component
- an index.js file which handles exporting the component to the rest of the app
